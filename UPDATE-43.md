# Update 43 — очистка дублирующихся slash-команд

- Удалён устаревший файл команды `commands/achievments.js`.
- Оставлена одна команда `/achievements`.
- `npm run deploy:prod` теперь сначала удаляет старые глобальные команды.
- Перед публикацией очищается старый серверный набор и загружается один новый комплект.
- Добавлена проверка одинаковых имён команд внутри папки `commands`.
- Добавлена команда полной очистки: `npm run deploy:clean`.

После обновления выполнить:

```bash
npm run deploy:prod
```

Затем перезапустить бота и полностью перезапустить Discord (Ctrl+R либо закрыть приложение и открыть снова).
