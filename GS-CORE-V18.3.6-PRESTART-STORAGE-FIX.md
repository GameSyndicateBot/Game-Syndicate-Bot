# GS CORE V18.3.6 — PRESTART STORAGE FIX

- Очистка старых локальных бэкапов запускается до копирования data-файлов и до открытия основной SQLite базы.
- При старте удаляется безопасный `database.sqlite-shm`, затем выполняется `wal_checkpoint(TRUNCATE)`.
- При критически малом свободном месте остаётся только самый свежий локальный бэкап.
- Голосовой тик заранее проверяет свободное место и временно пропускает необязательную запись вместо `SQLITE_FULL`.
- Ошибка удалённой Discord-роли достижения безопасно пропускается.
- Включены все изменения V18.3.2–V18.3.5.
