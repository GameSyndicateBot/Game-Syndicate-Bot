const { AuditLogEvent }=require('discord.js');const{sendLog}=require('../utils/sendLog');const{getAuditExecutor}=require('../utils/getAuditExecutor');const{execName}=require('../utils/logFormat');module.exports={name:'roleDelete',async execute(r){const a=await getAuditExecutor(r.guild,AuditLogEvent.RoleDelete,r.id);await sendLog(r.guild,{section:'Сервер',title:'Роль удалена',color:0xEF4444,fields:[{name:'Роль',value:`@${r.name}  \`${r.id}\``,inline:false},{name:'Удалил',value:execName(a),inline:false}]});}};