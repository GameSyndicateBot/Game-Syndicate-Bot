const { AuditLogEvent }=require('discord.js');const{sendLog}=require('../utils/sendLog');const{getAuditExecutor}=require('../utils/getAuditExecutor');const{execName}=require('../utils/logFormat');module.exports={name:'roleCreate',async execute(r){const a=await getAuditExecutor(r.guild,AuditLogEvent.RoleCreate,r.id);await sendLog(r.guild,{section:'Сервер',title:'Роль создана',color:0x22C55E,fields:[{name:'Роль',value:`${r}  \`${r.id}\``,inline:false},{name:'Создал',value:execName(a),inline:false}]});}};