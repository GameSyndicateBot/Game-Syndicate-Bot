const { AuditLogEvent }=require('discord.js');const{sendLog}=require('../utils/sendLog');const{getAuditExecutor}=require('../utils/getAuditExecutor');const{execName}=require('../utils/logFormat');module.exports={name:'channelUpdate',async execute(o,n){const x=[];if(o.name!==n.name)x.push(`Название: **${o.name}** → **${n.name}**`);if(o.parentId!==n.parentId)x.push(`Категория: ${o.parent||'—'} → ${n.parent||'—'}`);if('topic'in o&&o.topic!==n.topic)x.push('Тема канала изменена');if(!x.length)return;const a=await getAuditExecutor(n.guild,AuditLogEvent.ChannelUpdate,n.id);await sendLog(n.guild,{section:'Сервер',title:'Канал изменён',color:0x3B82F6,fields:[{name:'Канал',value:`${n}  \`${n.id}\``,inline:false},{name:'Изменения',value:x.join('\\n'),inline:false},{name:'Изменил',value:execName(a),inline:false}]});}};