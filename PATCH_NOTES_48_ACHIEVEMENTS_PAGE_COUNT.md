# Patch 48 — Quick Events achievements pagination

- Карточка категории теперь использует тот же размер страницы, что и команда: 8 достижений.
- Все 8 достижений Quick Events помещаются на одной карточке.
- Надпись страницы теперь показывает `Страница 1 / 1`.
- Кнопка «Вперёд» отключается, потому что второй страницы больше нет.
- Заголовок категории отображается как `Quick Events`, а не `quick_events`.
